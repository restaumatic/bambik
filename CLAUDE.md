# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Bambik is a prototype PureScript library implementing **Profunctor User Interfaces** - a novel approach to declarative Web UI development using Material Design Components. The key insight is that profunctors unify optics (data structure navigation) and arrows (data flow), making them ideal for composable UI development.

## Build Commands

Do `export PATH=$PWD/node_modules/.bin:$PATH` and then `spago build` (tests: `spago test`). Note the repo builds with the forked PureScript compiler pinned in `package.json` (variant row sugar `[ l :: T | r ]`, `.label` constructors — see doc/variant-sugar.md), so `npm install` first.

**Agent loop: use watch mode.** When introducing code changes, keep `spago build -w` running in the background and read its output after each edit (~0.7s incremental) instead of one-shot `spago build`s per change — plain `spago build -w` covers library, tests, and all 7GUIs and nGUIs demos (they are project sources; the `Main`-module demos — 1, 2, helloworld — still need their own `--path`, one at a time). Caveats: spago -w reads stdin and dies on EOF, so keep stdin open (never `</dev/null`); run only one watcher over the shared `output/` at a time. Parallel subagents sharing the repo must serialize compiles (`flock <lockfile> spago build`) — and when a serialized build fails, check the per-module `Compiling` lines for *whose* module failed before assuming it's yours.

Dev loop: `npm run dev <demo>` (`counter`, `cells`, ..., or `1|2|helloworld`) serves the demo at `http://127.0.0.1:1234/` with auto-rebuild and browser auto-reload (~2s per edit; scripts/dev.mjs — mtime-polling watcher over src/ and the demo, spago incremental build, esbuild watch, reload via esbuild's `/esbuild` SSE endpoint). Runtime emission trace: set `window.__bambikTrace = true` (or `localStorage.setItem("bambik-trace", "true")`) in the browser console to log every propagation decision — stage-to-stage flow, `looped` re-feeds/swallowed echoes, and gate-withheld emissions (the otherwise-invisible ones).

## Building & Deploying Demos

1. Verify the forked compiler: `node_modules/.bin/purs --version` must report `0.15.16 [development build ...]`; if it shows stock `0.15.15`, run `npm install` (stock purs fails with "Module Prim.Variant was not found").
2. Bundle for deploy via the npm `bundle-demo-*` scripts (`bundle-demo-1|2|7guis|nguis`, or all at once: `bundle-demos`); the `demo-*` scripts are watch-mode, not for deploys. The 7guis and nguis demos are named modules (`Counter`, `Cells`, `TodoMvc`, ...) entered at their own function (`counter`, `cells`, `todoMvc`, ...), so they compile together (no clashing `Main`) and bundle via scripts/bundle-7guis.mjs / scripts/bundle-nguis.mjs, which synthesize the esbuild entry per demo — `spago bundle-app` can only call `Main.main`.
3. Deploy: `npm run deploy-demo-1|deploy-demo-2|deploy-demo-7guis|deploy-demo-nguis` — each scps the demo dir to host `xyz` (root@erykciepiela.xyz, see `~/.ssh/config`) at `/var/www/html/bambik/demo/`.
4. Verify: `http://erykciepiela.xyz/bambik/demo/<d>/` returns 200 (plain HTTP only).

## Architecture

### Core Type

```purescript
newtype PUI m i o = PUI (m { toUser :: i -> Effect Unit, fromUser :: (o -> Effect PropagationStatus) -> Effect Unit })
```

- `i` - input type (data model to display)
- `o` - output type (data model to capture)
- `toUser` - pushes model updates to UI
- `fromUser` - captures user interactions

### Key Source Files

- **src/PUI.purs** - Core UI profunctor type with class instances `Profunctor`, `Strong`, `Choice`, `Semigroupoid`, `Category` (`identity` is the echo wire), the four row merges (`RecordToRecord`, `RecordToVariant`, `VariantToRecord`, `VariantToVariant`), the two mixed strengths (`Resolving`, `Retaining`), and the **trace quartet** — `Costrong`/`Cochoice` (ecosystem duals of `Strong`/`Choice`: state feedback and iteration; knowledge-gated) and the coined `Coresolving`/`Coretaining` (terminating fold, productive unfold; each co-strength is its strength's retraction, `co (strength g) ≅ g` once the state channel is primed) — plus the leaf combinators: `silence` (the silent widget), `announce` (the announcing constant — one registration emission of a value; seeds fields/cases and primes gated traces, generalizing the units' `{}` announcement), `with` (seed any stage with an initial value — `with a w = seeded a >>> w`, insertable at every `PUI m a b` position: initial models at the entry — `body $ with initial $ ...` — operand seeding, trace priming), `seeded` (the seeded echo wire — `identity`'s pass-through plus one registration emission of the seed; primes gated traces from inside their inner chain) `updates` (the Mealy update stage: a pass-through wire that folds each event emission of a wrapped `×→+` widget into the retained value — events carry bare payloads; with `looped` it is the model–view–update shape), `displayed` (make any display an unconditional pass-through stage — for **non-collection** passthroughs, a static chrome sibling or a `provided` pane line, and for a terminal **collection** display written `item # foreach _.key # lcmap proj # displayed`, where `displayed`'s unconditional carrier echo *is* the sequence merge's announcing unit — so an empty collection never starves), `constantly` (pin a stage's input to a known value, input type left free — constant-fed catalogues), `muted` (embed `{}`-typed chrome at any position, emissions dropped — static nav in a live `drawer` slot), `every` (the heartbeat wire: pass-through plus a periodic `a -> Maybe a` step — tick sources), `mvu` (the model–view–update shape, named: `mvu seed w = looped (with seed w)` — the standalone-app stage, so demos read `body $ ... $ pipeline # mvu seed`) and `looped` (the `×`-diagonal **self-trace**: feed a widget its own emissions, re-entrancy-guarded — primitive because a gated `unfirst` cannot self-feed; wrapped around a record merge it supplies sibling cross-feed, with per-operand retention falling out of the merge gates). `synced` and `latch` are DELETED — ensembles are now `dimap`-bracketed `looped` record merges. `PUI` also **re-exports the adopter family** (`asField`/`forField`/`forValue`/`projection`/`field`/`focusRecord`/`completed`/`tapped`/`asCase`/`forCase`), so demos import the row modules only for the `.do` merges and the trace forms. The module header carries the pipeline-semantics doc (how an emission travels and loops); doc/type-errors.md catalogs the archetypal row-layer compile errors with reproduced output
- **src/PUI/Web.purs** - the carrier: DOM monad (`Web = StateT DOM Effect`), `Node`, DOM building blocks (`element`, `attachable`, `runDomInNode`) and FFI — no widgets
- **src/PUI/HTML.purs** - the 1-1 HTML vocabulary over the carrier: primitive elements (`text`, `input`, `button`, `div`, etc.); entry `body` (registers the widget's wiring and feeds nothing — initial data enters via `with initial`/`announce`/`seeded`; the standalone app reads `body $ with initial $ ...`); collection & interaction vocabulary: `foreach :: (a -> String) -> PUI Web a o -> PUI Web (Array a) o` (the single collection combinator — the **sequence merge**, `sequenced` from `Data.Profunctor.Row.Sequence`; **keyed and retaining**: written trailing `item # foreach _.key`, it reconciles by key — matched elements re-fed in place, new keys built, absent removed, DOM reordered only when the key sequence changed, so a fixed-key grid never rebuilds, a growing list appends, and a reordered list **moves each element's DOM node with it** so focus/scroll/local state follows the item; keys must be unique; a re-entrancy guard stops an element echo from double-building mid-reconcile), `attrWith :: String -> (i -> String) -> ...` (value-computed attribute — the channel-fed counterpart of static `attr`/`:=`, so a cell's style/coord/colour updates in place through the channel rather than a closure rebuild), `clicked` (click emitter for any element — `button`'s replay-last-value protocol), `provided` (the view-model conditional — `PUI Web a b -> PUI Web (Maybe a) b`, content attached and fed on `Just`, detached on `Nothing`; pair with a named `Maybe`-valued projection — `pane # provided # lcmap currentQuestion` — so the pane consumes the payload, never the whole model, and visibility logic lives in testable business code; detaches, so a pipeline stage, not a gated-merge operand), `clWhen` (value-dependent class — styling, deliberately last-element-only); announcing statics `staticText`, the void `hr` leaf (`{} → {}` chrome) and `staticHTML` (raw-HTML string — internal MDC-chrome use only); the full element-ocular set (`div`/`span`/`table`/`tr`/`td`/`ul`/`li`/`p`/`h1`–`h6`/`svg`/`circle`/`path`/`img`/`strong`/`em`/`code`/`blockquote`/…, plus the generic `el` for computed/dynamic tags) with attribute/class decorators (`attr`/`:=`, `cl`) — SVG works because `element` is namespace-aware (`PUI.Web.childNS`/`namespaceURI`: `svg` opens the SVG namespace, children inherit, only SVG-namespaced elements use `createElementNS`; HTML stays on `createElement` so MDC component init is unaffected); **structure computed from data at runtime** is `PUI Web` all the way down, no markup DSL. Two regimes: (1) when only *values* change over a **fixed structure** (grids, the SVG canvas), feed the structure as data through the retaining `foreach` and compute each element's content/attributes from its fed value — `text # lcmap …` for content, `attrWith` for style/coords, `clicked … # rmap _.key` to emit identity — so cells are built once and updated in place (no wholesale rebuild, no `data-*`); (2) when an element's *structure* genuinely **varies with the data** (markdown blocks), the closure builders `foreachWith :: (a -> PUI Web {} o) -> PUI Web (Array a) o` (build a whole widget per element, `el ("h" <> show level)`, wholesale rebuild per feed), `dynamic` (its single-value case; a terminal `displayed (dynamic \m -> each …)` renders a projected structure and passes the model through), and `each xs build` (a fixed closure-known list pinned to `{}`) rebuild per value; each owns its container like `foreach`. `onClickedXY` (pointer-down local/viewBox `{ x, y }`) is the container-level coordinate emitter for canvases; `clicked` (fed value) and `onClickedXY` stay live across feeds — adopt the bare payload with `toCase`/`rmap`. `input`/`textArea` are focus-guarded: model updates never clobber the field being typed in, and the channel stays live
- **src/PUI/MDC.purs** - Material Design 2 components (the full https://m2.material.io catalog as far as MDC Web implements it), a two-sorted vocabulary. **Components** are widgets with a model interface, each a citizen of exactly one row direction — `×→×` editors (`filledTextField`/`debouncedTextField` — **canonically labeled** `{ value :: String }` editors, adopted per business field via `# asField @l` (RecordToRecord: renames `value`→`l`, `dimap`-only; sibling of `field`, which *wraps*; displays adopt via `# projection f # forField @l` / `# forValue # forField @l` — `lcmap`-only, reads one field into the bare-value display, owns no output); likewise `filledTextArea`, `checkbox`, `toggleSwitch`, `slider` (emits on **commit** only — one emission per adjustment, so an `updates` fold sees each drag as one transaction; `sliderLive` emits mid-drag for live readouts), `filterChip`, `iconToggle`, and the type-changing `radioButton`, `select`, `segmentedButton` — `{ value :: Maybe a } → { value :: a }`; `tabBar` is the same-type selector with unconditional echo and `MDCTab` activation — the `looped`-ensemble citizen) and the `{ busy } → {}` displays `indeterminateLinearProgress`/`indeterminateCircularProgress` and the determinate `linearProgress` (`{ value :: Number } → {}`, filled fraction 0–1 — the gauge shape `linearProgress # projection fraction`); the `×→+` events `button`/`fab`/`iconButton`/`menuItem` (canonical `[ clicked :: r ]`, adopted via `# asCase @l` — RecordToVariant, renames the case); the `+→×` statuses `snackbar` (auto-dismissing) and `banner` (own dismiss action) — canonical `[ event :: String ]` in, adopted via `# forCase @l` (VariantToRecord, renames the incoming case) — no scalar or polymorphic component interfaces (raw scalar leaves are private; `Web` primitives stay scalar for optic positions). `listOf` is the MD2 list as a **dynamic collection component** (`PUI Web a o -> PUI Web (Array a) a`: `foreach`-retained items (reconciled, not rebuilt), MD2 selected styling via the optional `selected` predicate — `listOf {}` selects nothing — each item a click emitter replaying its value). The module header states the **`dimap` round-trip contract** for editors (lossy conversions belong in the model via `rmap` after `completed`, not in leaf brackets). **Oculars** (`card { caption }`, `dialog`/`simpleDialog` (modal protocol: **open on feed, close on emission** — feed selectively behind an event case; the content's final stage must emit only on decision, `simpleDialog`'s built-in confirm is a `clicked` pass-through replaying the content's last output), `menu { label }`, `chipSet`, `list`/`listItem`, `dataTable { columns }`/`dataRow`/`dataCell`, `imageList`, `layoutGrid`/`layoutCell`, `topAppBar { title }`, `drawer` (permanent, with a **live nav slot** — nav and content are sibling stages over the same types, both fed, either side's emissions exit; static chrome nav embeds via `muted`), `tooltip { text }`, typography, elevations) are shape-preserving decorators with no model of their own; `divider`/`imageListItem` are announcing statics (`{} → {}` chrome with a face). Internally the live leaf is `field @l`-lifted (preferred over raw `property` for annotation-free inference; runtime-exactness is enforced by the merge gates' `MergeableRecords` trim) with hand-fused chrome (abstract labels can't flow through the merges' `Nub`), while all-chrome groups have concrete rows and stay literal `RecordToRecord.do` merges of announcing chrome (`staticText`/`staticHTML`/`pempty` at `{} → {}`); code order = DOM order
- **src/Data/Profunctor/** - `Cont` (CPS profunctor) + the `Row/` layer; everything else was dissolved or deleted
- **src/Data/Profunctor/Row/** - Row profunctors over `Record`/`Variant`: four direction modules, each carrying its **direction class** — the binary merge plus its nullary unit `pempty`, the genuine per-carrier primitives — with qualified-do sugar (`bind`/`discard`). Everything kept is demo-reachable; laws are stated in the module headers. Type variables follow the photographic schema: focus `f`, background `b`, shot `s` (`Cons l f b s`), reality `r`.
  - **`RecordToRecord.purs`** (×→×) — merge `recordToRecord` (`SharedRecordInputs` + `OwnedRecordOutputs`; gated on `PUI`); over ecosystem `Strong`: `focusRecord` (sub-record focus, background carried), `property` (the type-changing field lens); over bare `Profunctor`: `field` (`property`'s closed-singleton form — the merge-operand/nesting shape, `dimap`-only, annotation-free under the merges; runtime-exactness is enforced by the merges themselves — their `MergeableRecords` evidence trims every operand emission to its declared output row before the gates' left-biased `Record.union`, so lens-rebuilt emissions carrying stale sibling fields can't shadow fresh values); `required` (marks a type-changing selector as always-selected — dissolves the `dimap (\v -> { value: Just v }) _.value` bracket: `select config options # required # asField @l`); `tapped` (a `Strong`-derived display tap: shows the value flowing through a pipeline stage and passes it on — the display's echo triggers the forwarding; honest over displays only); `completed` (complete a widget's output to its full input row over `Strong` — unproduced fields carried from the retained input, `FieldNames`-trimmed union, absorbs the input widening; kills `field @l identity` echo wires); over ecosystem `Costrong`: the **`Colens`** optic with `colens`/`colensE` (the reversed lens — each input read against the last output) and its row form `feedback` (the ×-trace at row granularity — a state sub-record loops from output to input; coerce-split like `focusRecord`, so pipeline stages only, and on `PUI` the state must be primed by the widget's first emission).
  - **`VariantToVariant.purs`** (+→+) — merge `variantToVariant` (`OwnedVariantInputs` — one handler per case — + `SharedVariantOutputs`); over ecosystem `Choice`: `case_` (the value-level case prism, via `prismE`) and `splitVariant` (the dispatch helper `reelWrap` and `iterate` share); over ecosystem `Cochoice`: the **`Coprism`** optic with `coprism`/`coprismE` (the reversed prism — every result exits or re-enters as the next focus, `tailRec` at the optic level) and its row form `iterate` (the `+`-diagonal trace at row granularity — `again` cases loop back into the input, `done` cases exit; retry/wizard flows).
  - **`RecordToVariant.purs`** (×→+) — strength `Resolving`/`resolve :: p a b -> p (Tuple a c) (Either b c)` (a loop step: `Left` = Done, `Right` = Loop; `PUI`-only instances — the branch is derived **from time**: emissions loop while the widget is still moving, the last resolves at quiescence, so `coresolve (resolve g) = debounced g`; window parameterized via `resolveFor`) and its co-strength `Coresolving`/`coresolve` (ties the loop: a terminating fold) with its row form `folding @w` (case `w` continues the fold silently, `done` cases exit — the accumulating-wizard shape); merge `recordToVariant` (ungated broadcast); over `Resolving`: `resolveProperty`, `propertyToCase`, `shutterWrap`, the **`Shutter`** optic with `shutter`/`shutterE`, and over `Coresolving` the **`Coshutter`** optic with `coshutter`/`coshutterE` (the reversed `Reel` — the fold state is a reader, `folding` is its row form); plus `recordToCase` (introduce an output case; plain `Profunctor`, the unit-pinned merge by law), `toCase` (`recordToCase` freed of the record-input constraint at the closed singleton row — introduce a **bare** output as case `l`, the output-side dual of `onCase` and the general sibling of `asCase`; dissolves the `rmap (\e -> .l e.field :: [...])` lambda at collection sites: `listOf {} item # rmap _.key # toCase @"picked"`) and `echoCase` (`recordToCase` over the echo wire at the closed singleton row — the `field` lesson at `×→+`, annotation-free as a merge operand).
  - **`VariantToRecord.purs`** (+→×) — strength `Retaining`/`retain :: p a b -> p (Either a c) (Tuple b c)` (a Mealy/coroutine step; `PUI`-only instances — a stateless function can't retain state) and its co-strength `Coretaining`/`coretain` (ties the state channel: a productive unfold/generator) with its row form `unfolding @w` (value fields pass, state fields resume as case `w`); merge `variantToRecord` (gated like ×→×, retaining the other side's last contribution); over `Retaining`: `retainCase`, `caseToProperty`, `caseToRecord` (Mealy reducer), `reelWrap`, the **`Reel`** optic with `reel`/`reelE`, and over `Coretaining` the **`Coreel`** optic with `coreel`/`coreelE` (the reversed `Shutter` — every emission leaves and resumes, `unfolding` is its row form).
  - **`Sequence.purs`** (module `Data.Profunctor.Row.Sequence`) — the **sequence direction**: the collection as the runtime-sized homogeneous merge. `class Profunctor p <= Sequencing p where sequenced :: (a -> String) -> p a o -> p (Array a) o` — keyed reconciliation, with the law in the header (singleton ≅ the element; empty ≅ silent unit; reuse retains identity by key). `PUI Web`-only instance (a DOM collection has no `(->)`/general-carrier meaning, like `Resolving`/`Retaining`); `PUI.HTML.foreach = sequenced`.
  - **`Row.purs`** (module `Data.Profunctor.Row`) — the shared floor: the row-constraint vocabulary (`InclusiveRows`/`ExclusiveRows` + the runtime-evidence duals `DispatchableVariants`/`MergeableRecords` with `exactRow`, bundled into the per-side classes `SharedRecordInputs`/`SharedVariantOutputs`/`OwnedVariantInputs`/`OwnedRecordOutputs` — sharing is inclusive, responsibility is exclusive, evidence only on owned sides — so each merge signature is two words, one per side; the owned sides also carry the `DisjointLabels` detector, which turns a duplicated label into a custom compile error naming the label — doc/type-errors.md #2) + the two `dimap`-only widening reshapings (`widenRecordInput`/`widenVariantOutput`) the `PUI` merge instances build on.
  - **`Row/Example.purs`** — a phantom carrier with label-indexed widget signatures; **`test/Main.purs`** — value-level `(->)` tests (`focusRecord`/`property`/`recordToCase`) plus merge unit laws, gating, and the trace quartet and its row forms (`unfirst`/`unleft`/`coresolve`/`coretain`/`looped`/`iterate`/`feedback`/`folding`/`unfolding`) on the `PUI` carrier via a probe harness; **`test/HelloShutterReel.purs`**/**`BusinessOptics.purs`**/**`RestaurantReel.purs`**/**`EntityEventExample.purs`** — `Shutter`/`Reel` as business optics.

### Composition Patterns

- `Semigroupoid.do` (qualified-do) - Data flow pipelines
- `RecordToRecord.do` / `RecordToVariant.do` / `VariantToVariant.do` / `VariantToRecord.do` (qualified-do) - the four row merges
- label-indexed components - every MDC component is a citizen of one direction (`filledTextField @l` ×→×, `button @l` ×→+, `snackbar @l` +→×), so pipeline stages are written directly from components; `field`/`focusRecord` nest sub-composites into larger aggregates
- variant editing - **record-shaped editor state**: the model keeps the variant, the editor keeps every payload; `dimap` brackets the variant in (a state function seeding absent payloads) and out (a projection on the selection), around `looped Semigroupoid.do { selection component # completed; payload panes }` — each pane a payload-typed editor stage `# provided # lcmap <paneOf> # updates <setPane>`, shown by the presence of its `Maybe` payload; consistency via the self-trace re-broadcast; unit-payload variants need only the bracket around one selection component
- conditional visibility - always a named `Maybe`-valued projection + `provided` (never an in-UI predicate): conditional *data* (a pane whose content only exists sometimes) → `pane # provided # lcmap <maybeOf>`; conditional *mode of a live editor* inside a `looped` ensemble → a payload pane as above. `clWhen` stays predicate-driven — it toggles a class (styling), not visibility
- `tapped` - live views as pipeline stages (slider readouts, summary lines, data tables): display every emission flowing through, pass it on

### Separation of Concerns

- **Business Logic** - Row-shaped models, structural as far as readable (anonymous Record rows for all-at-once, anonymous Variant rows for one-at-a-time; a named alias only for the top aggregate) plus plain functions and Aff actions (demo/1/Main.purs); business optics (Shutter/Reel) where state/loop semantics are needed (test/BusinessOptics.purs)
- **Design System** - Oculars in Web.purs and MDC.purs
- **Composition** - UI elements compose orthogonally to the row combinators

## Demo Structure

- **demo/1/** - Full MDC-based order form as the four-direction row pipeline: load action → `×→×` form (nested record merges; variant editors as `dimap`-bracketed `looped` pipelines of `tabBar`/`segmentedButton` + `provided` payload panes) → `tapped` live summary → `×→+` event buttons → `+→+` backend dispatch → `+→×` status snackbars
- **demo/2/** - Plain HTML demo: the whole page as one `×→×` merge — announcing statics around a minimal record merge over plain `input`s, drained into `silence`
- **demo/helloworld/** - Simple intro example
- **demo/7guis/** - The seven [7GUIs](https://eugenkiss.github.io/7guis/) benchmark tasks, one page each (counter, temperature-converter, flight-booker, timer, crud, circle-drawer, cells), all on the named MVU shape `pipeline # mvu seed`; circle-drawer's SVG canvas and cells' 31×27 spreadsheet grid are **channel-fed structure-from-data** in `PUI Web` (a fixed canvas/grid fed as data through the retaining `foreach` — cells/circles built once and updated in place via `attrWith` + `text`, so an edit/selection/resize never rebuilds the ~800 cells or redraws the canvas; circle-drawer uses container-level `onClickedXY`, cells emits its clicked-cell key via `clicked … # rmap _.key` — no `data-*`, no markup DSL), `MDC.listOf` for crud's list, a `FlightType` ADT through the type-changing `select` (`# required # asField`) in flight-booker, circle-drawer's live-preview resize via `sliderLive` with an `adjusting` flag coalescing each drag session into one undo transaction, a both-fields model in temperature-converter (non-numeric input leaves the other field untouched) and a hand-rolled formula evaluator in cells; each page shows its own Main.purs beside the running demo. Verify with the headless-Chrome CDP harness (bundle + http.server + scratchpad cdp.mjs)
- **demo/nguis/** - Companion set of popular showcase apps, same conventions as 7GUIs (named modules entered at their own function, per-page source panels, scripts/bundle-nguis.mjs): todomvc (`listOf` click-to-toggle, `clWhen` done-styling, `segmentedButton` filter), tip-calculator (all-`×→×` form, sliders, `tapped` money readouts), quiz (`provided` panes over multi-stage pipelines keyed on `Maybe`-projected question/outcome, `linearProgress`, `listOf` choices), tic-tac-toe and calculator (**channel-fed `foreach` grids** — cells built once, content via `text`, style via `attrWith`, key emitted via `clicked … # rmap`, folded by `updates`; no `dynamic` rebuild, no `data-*`; calculator's keypad is static keys fed once through the retaining `foreach`, readout channel-fed), markdown-previewer (`filledTextArea` + injection-proof preview as recursive `PUI Web` — `displayed (dynamic \doc -> each …)` over element oculars with `el ("h" <> show level)`, structure genuinely varies per block so it stays builder-built — over a hand-rolled parser), helloworld (`body $ staticText` — the 5 kB bundle floor), stopwatch (`every` tick pausing via `Nothing`, laps a keyed `foreach identity … # lcmap lapLines # displayed`), reorder (the keyed-reconciliation showcase: a playlist keyed by track id, each row a bare DOM-local checkbox; Rotate and the effectful Shuffle — `button # asCase` → `silence # action … # onCase`, `Effect.Random` — both move each row's DOM node with its track so the tick follows it), shopping-cart (`dataTable`/`dataRow`/`dataCell` over `foreach`, `constantly`-fed `listOf` catalogue), password-generator (effectful stage: `button # asCase` → `action`/`onCase` with `Effect.Random` → `updates`), color-mixer (`sliderLive` channels driving an `attrWith` swatch + static `foreach` preset chips). The **trace-quartet row forms** each get a focused single-combinator demo here: auction (`feedback`, `Costrong`: a `top`-bid field loops output→input inside a `seeded`-primed inner chain — the running high-water-mark, invisible in the stage's outer type), checkout (`folding @"next"`, `Coresolving`: a 3-step wizard whose step state loops silently as the `next` case, `announce`-primed, exiting on the `placed` done-case), payment (`iterate`, `Cochoice`: a flaky card charge whose failed attempt re-emits `charge` with attempt+1 until it succeeds), ticket-dispenser (`unfolding @"resume"` + `retain`, `Coretaining`: "take a number" — each press emits the next ticket, the counter retained and resumed as the `resume` case)

## Key Dependencies

- `profunctor-lenses` - Profunctor-based optics
- `qualified-do` - Syntax sugar for profunctor composition
- `material-components-web` - MDC JavaScript library
