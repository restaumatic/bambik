# Bambik

## Overview

A prototype PureScript library implementing **Profunctor User Interfaces** - a novel approach to declarative UI development. The key insight is that profunctors unify optics (data structure navigation) and arrows (data flow), making them ideal for composable UI development.

**doc/guardrails.md is normative**: the strict MUST/MUST-NOT rules for the library and for applications built on it, plus the admission test every proposed feature passes (derivation → laws → subsumption → honesty → reachability → green stack → sync). Its L15 makes the demo suites the compatibility contract: any library change, however internal, must leave every demo compiling, bundling, and behaving correctly — verified by running `spago build`, `spago test`, `npm run bundle-demos`, and `npm run smoke` in full, with demos edited only when the demo itself is the subject of the change. Consult it before adding, changing, or accepting any combinator, class, component, or demo idiom — a change that violates a guardrail is wrong even if it works. Its **L16 import tower** governs imports: only the algebra layer (`PUI`, `Data.Profunctor.*` in bambik) imports the ecosystem's `Data.Profunctor`; vocabulary modules (design systems, `PUI.Web.HTML`/`PUI.Web.SVG`, packaged controls) build from the carrier + the re-exported vocabulary; application code imports neither the ecosystem algebra nor carrier internals, and `grep "import Data.Profunctor (" demo/` stays empty. Its **Part II is a pointer, not a rulebook**: the code-style contract for application code — demos included — is stated once, in **`.claude/skills/developing-bambik-apps/writing.md`** (its *Code style* section: layout, types and values, business functions, wiring). Read that file before writing or reviewing any demo or app module; nothing here or in guardrails.md restates it, and a change to how applications are written edits writing.md and the demos. The one deliberate restatement is the demo pages' `#code-style` note in demo/index.html — deployed HTML cannot read the skill file, so re-read it against writing.md whenever the contract changes.

## Building

Do `export PATH=$PWD/node_modules/.bin:$PATH` and then `spago build` (tests: `spago test`). 

### PureScript forked compiler

Note the repo builds with the forked PureScript compiler pinned in `package.json` (variant row sugar `[ l :: T | r ]`, `.label` constructors — see doc/variant-sugar.md), so `npm install` first. Nothing in the toolchain depends on a local checkout, so a plain clone builds on any Linux x86_64 machine: the compiler installs from its **GitHub release** (`erykciepiela/purescript` tag `v0.15.16-variant.6`, built from branch `variant-type-sugar` — the release also carries the bare `purs` binary as an asset, and package-lock.json pins the tarball's integrity hash), and the `Prim.Variant`-patched variant library is an ordinary git package in packages.dhall (`with variant.repo`/`.version` → `erykciepiela/purescript-variant` tag `v8.0.0-prim-variant.1`, branch `prim-variant`: `Data.Variant` re-exports the compiler's built-in `Prim.Variant.Variant` instead of declaring its own, so the sugar and `inj`/`on`/`match` share one type). External applications consume bambik as an **ordinary spago git package** pinned to a release tag (`with bambik = { dependencies = [ … ], repo = "https://github.com/restaumatic/bambik.git", version = "v0.1.1" }`) — spago clones the repo and globs `src/**/*.purs`, so no checkout sits beside the app and the demos/docs ride along under `.spago/bambik/<tag>/`. The entry must spell out the library's dependency list (spago does not read a git package's own spago.dhall), but **nothing in the repo duplicates that list**: the bootstrap procedure fetches it from the pinned tag (`raw.githubusercontent.com/restaumatic/bambik/<tag>/spago.dhall`) when it writes the app's packages.dhall, so adding a dependency here needs no companion edit anywhere. A library release is a tag on this repo. The standalone bootstrap flow lives in `.claude/skills/developing-bambik-apps/` (bootstrap.md, which carries the scaffold as inline file contents rather than a stored template), self-contained and copyable out of the repo.

### Development loop - watch mode

When introducing code changes, keep `spago build -w` running in the background and read its output after each edit (~0.7s incremental) instead of one-shot `spago build`s per change — plain `spago build -w` covers library, tests, and the demos. Caveats: spago -w reads stdin and dies on EOF, so keep stdin open (never `</dev/null`); run only one watcher over the shared `output/` at a time. Parallel subagents sharing the repo must serialize compiles (`flock <lockfile> spago build`) — and when a serialized build fails, check the per-module `Compiling` lines for *whose* module failed before assuming it's yours. It's recommended to use git workspaces in such cases.

`npm run dev` serves **every** demo from one server at `http://127.0.0.1:1234/`, rooted at `demo/` in the same folder layout the deploy scps to the remote host — local `/7guis/counter-mdc2/` is `/bambik/demo/7guis/counter-mdc2/` there, so every relative link and asset path resolves identically in both places (the server hosts nothing but the demos, so it carries no `/bambik/demo` prefix of its own). The root landing page (demo/index.html) links the two suites. Narrow the bundling to what you're working on by name or set: `npm run dev counter-mdc2 cells-mdc2`, `npm run dev nguis`. Auto-rebuild and browser auto-reload throughout (~2s per edit; scripts/dev.mjs — one mtime-polling watcher over src/ and demo/ driving two paths by file kind: a `.purs`/`.js` edit runs an incremental `spago build` and esbuild's own watch over `output/` rebundles the affected demos and reloads once the new bundle lands, while a page's `.html` has nothing to compile and reloads immediately — both kinds are handled independently, so an edit to each in the same tick gets a reload *and* a rebuild; reloads reach the browser over an SSE endpoint at `/esbuild` using esbuild's own protocol, injected via the bundle banner so demo pages need no dev-only markup). Polling rather than `fs.watch`: inotify costs an instance per directory and Node hits its per-process ceiling well before the ~40 source dirs here, while `recursive: true` silently delivers no events at all on ext4. Runtime emission trace: set `window.__bambikTrace = true` (or `localStorage.setItem("bambik-trace", "true")`) in the browser console to log every propagation decision — stage-to-stage flow, `looped` re-feeds/swallowed echoes, and gate-withheld emissions with the sibling fields they wait for. Independent of the trace flag, every knowledge gate carries a **starvation watchdog**: a gate that withholds and is never fed within 3s prints one `console.warn` naming the gate and (for the record merges) the exact missing fields, with the fix (`seeded`/`announce`, or the seed argument of `feedback`/`folding`/`unfolding`) — so an unprimed gate is a named failure, not a blank screen (and an unprimed *entry* is now a compile error: `body` demands input `{}`) (off until a carrier adopts the host switches — `PUI.Web.adoptHostDiagnostics`, called at `body`/`runComponentInNode`, so a headless `spago test` is silent; opt out with `window.__bambikNoWarn = true`).

Verification stack: `spago test` (value-level law tests over probes — merge units, gating, exactness, the trace quartet); `npm run smoke` (scripts/smoke/ — the committed headless-Chrome CDP harness: serves the repo, launches an isolated throwaway Chrome, runs scripts/smoke/tests/*.mjs; this is where the **carrier-only laws** are tested — the container action's keyed reconciliation/identity-follows-key in the real DOM, the gather gate on a live page (potluck) — plus per-demo smokes (including order-form's Aff load → variant-pane → summary-tap walk); the value-level `Acting` laws (empty announces `[]`, singleton retraction, gather gate, keyed retention) run in `spago test` on `PUI Effect` probes; bundle the demos first, filter with `npm run smoke -- <name>`); `npm run api-docs` generates the browsable API reference into `generated-docs/md/` (gitignored) from the module headers — the single source of truth for combinator contracts.

## Building & Deploying Demos

1. Verify the forked compiler: `node_modules/.bin/purs --version` must report `0.15.16 [development build ...]`; if it shows stock `0.15.15`, run `npm install` (stock purs fails with "Module Prim.Variant was not found").
2. Bundle for deploy: `npm run bundle-demos` (minified, all demos; `node scripts/bundle.mjs <name|set>` for a subset) — use `npm run dev` for watch mode, not for deploys. **Every** demo is a named module (`OrderForm`, `Counter`, `Cells`, `TodoMvc`, ...) entered at its own function (`orderForm`, `counter`, `cells`, `todoMvc`, ...) — no module is `Main`, so they all compile together under one plain `spago build` — and each bundles from the shared registry in **scripts/demos.mjs** (the single source of truth for directory + module + entry, shared with the dev server), which synthesizes the esbuild entry per demo because `spago bundle-app` can only call `Main.main`.
3. Deploy: `npm run deploy-demos` — scps demo/index.html and both suite directories to host `xyz` (root@erykciepiela.xyz, see `~/.ssh/config`) at `/var/www/html/bambik/demo/`.
4. Verify: `http://erykciepiela.xyz/bambik/demo/<d>/` returns 200 (plain HTTP only).

Every demo page is pure structure over **shared chrome**: `demo/page.js` (loaded as `../../page.js`) fetches the listing named by `<body data-source="CounterMDC2.purs">` (space-separated filenames render several listings — the first fills the existing box, each further file gets a filename heading + listing, the header readout sums the sizes; order-dashboard-mdc3 shows its app and its packaged controls module this way), fills the header's source/bundle size readouts, and groups the running demo with its tracing note into one `#demo-column` — the demo mounts into `<body>` at runtime with no marker class of its own, so it cannot be wrapped in static markup and is collected once, then the observer disconnects. The two notes that used to be pasted into all 34 pages now live once in **demo/index.html** as `#code-style` and `#tracing` sections, linked from beneath the source box and beneath the running demo respectively.

## Cutting a release

A bambik release is a tag on this repo plus a release page. Four steps:

1. Bump the tag in the dependency table of `.claude/skills/developing-bambik-apps/bootstrap.md` **before** tagging, so the tagged tree documents its own tag. Nothing else in the scaffold carries a version — the packages.dhall step writes `<tag>` and fetches the library's dependency list from it, so a new library dependency needs no companion edit.
2. Verify green in full — `spago build`, `spago test`, `npm run bundle-demos`, `npm run smoke` (L15) — then tag the verified commit (`git tag -a v0.2.0`) and push tag and branch.
3. Create the release page from the tag. The body is **minimal by decision**: the skill-usage prompt naming this tag's asset URL, and nothing else — the toolchain pins, the packages.dhall entry and the prototype status live in the skill's bootstrap.md (which ships attached) and are not restated per release, so there is one place to keep them right. Use the **GitHub REST API**, not the `gh` CLI — `gh` is not installed here, and `GITHUB_TOKEN` is in the environment. Write the JSON with a script rather than inlining it in the shell, so the body's newlines and markdown survive quoting:

```sh
node -e 'require("fs").writeFileSync("body.json", JSON.stringify({
  tag_name: "v0.2.0", name: "bambik v0.2.0", draft: false, prerelease: false,
  body: "…the skill-usage prompt, naming the asset URL for this tag…" }))'
curl -s -X POST -H "Authorization: Bearer $GITHUB_TOKEN" \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/restaumatic/bambik/releases -d @body.json
```

   The response's `upload_url` (strip its `{?name,label}` suffix) is where step 4 posts. A `message` field in the response means it failed — check it; curl exits 0 on API errors.
4. Attach the authoring skill as an asset, built **from the tagged tree** so it cannot drift from the library it documents:

```sh
mkdir -p /tmp/skillpack && cd /tmp/skillpack
git -C <repo> archive v0.2.0 .claude/skills/developing-bambik-apps | tar x --strip-components=2
tar czf developing-bambik-apps-v0.2.0.tar.gz developing-bambik-apps
curl -s -X POST -H "Authorization: Bearer $GITHUB_TOKEN" \
  -H "Content-Type: application/gzip" \
  --data-binary @developing-bambik-apps-v0.2.0.tar.gz \
  "<upload_url>?name=developing-bambik-apps-v0.2.0.tar.gz"
```

   Then verify the published result rather than assuming it: re-fetch `releases/tags/v0.2.0` (asset `state` must be `uploaded`, `draft` false) and run README's install one-liner into a throwaway directory.

The asset is a snapshot, so it needs re-attaching per tag — that is its one cost, bought for a portable one-command install (`tar xz -C .claude/skills`, no GNU-only flags) and a visible place to find it. README's install command and release link need the new tag too. This procedure lives here, not in the skill: cutting a release is a library-maintainer task, and the skill ships to application developers who never do it.

## Architecture

### Core Type

```purescript
newtype PUI m i o = PUI (m { toUser :: i -> Effect Unit, fromUser :: (o -> Effect Unit) -> Effect Unit })
```

- `i` - input type (data model to display)
- `o` - output type (data model to capture)
- `toUser` - pushes model updates to UI
- `fromUser` - captures user interactions

### Key Source Files

- **src/PUI.purs** - Core UI profunctor type with class instances `Profunctor`, `Strong`, `Choice`, `Semigroupoid`, `Category` (`identity` is the echo wire), the four row merges (`RecordToRecord`, `RecordToVariant`, `VariantToRecord`, `VariantToVariant`), the two mixed strengths (`Resolving`, `Retaining`), and the **trace quartet** — `Costrong`/`Cochoice` (ecosystem duals of `Strong`/`Choice`: state feedback and iteration; knowledge-gated) and the coined `Coresolving`/`Coretaining` (terminating fold, productive unfold; each co-strength is its strength's retraction, `co (strength g) ≅ g` once the state channel is primed) — plus the leaf combinators: `silence` (the silent UI component), `announce` (the announcing constant — one registration emission of a value; seeds fields/cases as a merge operand, generalizing the units' `{}` announcement), `with` (**discharge a UI component's initial-state obligation** — `with a w = announce a >>> w :: PUI m {} b`: the entity `w` edits exists from the very beginning, `a` is its initial state, and supplying it closes the input to `{}`, the type `body` demands — `body $ with initial $ ...`), `seeded` (the seeded echo wire — `identity`'s pass-through plus one registration emission of the seed; the method of `class Category p <= Seeding p` in **`extras/row-profunctors/Data/Profunctor/Seeding.purs`**, the pointedness-as-carrier-structure primitive the seeded trace forms build on, re-exported from `PUI`; still the tool for pass-through seeding stages and raw `unfirst`/`colens` chains) `updated` (the Mealy update stage: a pass-through wire that folds each event emission of a wrapped `×→+` UI component into the retained value — events carry bare payloads; with `looped` it is the model–view–update shape; **both sides subsume**: the handler may touch a sub-row of the model and the wrapped event UI component may be fed a sub-row of it, so closed narrow business signatures need no coercion stage — the type-agnostic form is the private `mealy`), `displayed` (make any display an unconditional pass-through stage — for **non-collection** passthroughs, a static chrome sibling or a `provided` pane line, and for a terminal **collection** display written `item # foreach @"id" proj # displayed`, where `displayed`'s unconditional carrier echo *is* the sequence merge's announcing unit — so an empty collection never starves), `observed` (make a status an **event pass-through stage** — `displayed`'s sibling on the `+`-diagonal: every event forwards exactly once at feed time, the status shows the cases it consumes, and subsumption runs the variant way (`Contractable` narrowing — the status may consume a narrower row, background cases pass untouched); the status's own emissions are dropped, deliberately — events are one-shot, an echo re-emission would duplicate them; payment's retry toast `snackbar # forCase @"event" @"charge" retryLine # observed` narrates the `iterate` loop without interrupting it), `informed` (the **dispatch adapter**: make a single-record business function an `updated` handler or `match` branch — the event's payload laid over the retained model row, fresh knowledge wins via left-biased union, reads per-branch exact by subsumption — `match { refunded: informed applyRefund }` over `applyRefund :: { amount, balance } -> { balance }`; dissolves the `payload -> model -> model` currying, A12), `constantly` (pin a stage's input to a known value, input type left free — constant-fed catalogues; a *leading* pipeline stage has no neighbour to pin it, so there the consuming mechanism's projection argument states the `{} -> …` footprint), `optional` (mark a type-changing selector as **possibly unselected** — the dual of `required @"value"`: completes the selector's `Just`-only leaf echo with the `Nothing` half and wraps picks in `Just`, so the model keeps the `Maybe`, an unmade choice flows as honest knowledge instead of starving the merge gate, and the stages demanding the bare selection stay `provided`-gated until the user picks — the no-defaults discipline: `dropdown config options # optional # asField @"value" @l` seeds as `Nothing`), `every` (the heartbeat wire: pass-through plus a periodic step over a **sub-row** of the model, merged back over the last value — tick sources; the period is structural `{ ms :: Number }`, as are all the vocabulary's durations — `debounced`, `resolveFor`, `inputDebounced`, `debouncedTextField` — never `Milliseconds`; the type-agnostic form is the private `heartbeat`), `mvu` (the model–view–update shape, named: `mvu seed w = with seed (looped w) :: PUI m {} model` — the model is an entity, `seed` its initial state, and supplying it **closes** the app, so demos read `body $ ... $ pipeline # mvu seed`) and `looped` (the `×`-diagonal **self-trace**: feed a UI component its own emissions, re-entrancy-guarded — primitive because a gated `unfirst` cannot self-feed; wrapped around a record merge it supplies sibling cross-feed, with per-operand retention falling out of the merge gates). `synced` and `latch` are DELETED — ensembles are now `bracketed` record merges (the variant-editor bracket). `PUI` also **re-exports the adopter family** (`asField`/`forField`/`forProperty`/`atField`/`atProperty`/`projected @"value"`/`field`/`toField`/`subStrong`/`subChoice`/`completed`/`tapped`/`asCase`/`toCase`/`toCases @"clicked"`/`atCase`/`forCase`/`forCases @"event"`, plus `acted`/`optioned` from `Data.Profunctor.Acting`), so demos import the row modules only for the `.do` merges and the trace forms. The module header carries the pipeline-semantics doc (how an emission travels and loops)
- **src/PUI/Trace.purs** - the **development diagnostics**, extracted so the core type module carries no foreign import of its own: `tr` (the emission trace) and `gateGuard` (the one-shot starvation watchdog), used ~30 times inside `PUI`'s gated combinators and exported from neither. The two switches **and the log sink** are all parameters (`setTracing`/`setDiagnostics`/`setSink`, no-ops until installed), so the module has **no JavaScript of its own** — every `.js` in the library lives under the Web layer. A carrier installs them: `PUI.Web.adoptHostDiagnostics` passes the browser console and reads `window.__bambikTrace`/the `bambik-trace` local-storage key/`window.__bambikNoWarn`, at the mount entries (`body`, `runComponentInNode`). `tr` is polymorphic in the logged value and a `Ref` cannot hold a `forall`, so the value crosses to the sink as the opaque `Logged` (a `foreign import data`, which needs no foreign module). It cannot live in `PUI.Web` — `PUI.Web` imports `PUI`, and `PUI` calls `tr`, so that would be a cycle
- **src/PUI/Web.purs** - the carrier **and the root of the web layer**: DOM monad (`Web = StateT DOM Effect`), `Node`, DOM building blocks (`element`, `attachable`, `runDomInNode`) and FFI — no UI components. Everything browser-specific is a submodule of it: the element vocabularies `PUI.Web.HTML`/`PUI.Web.SVG` and one module per design system (`PUI.Web.MDC2`, `PUI.Web.MDC3`, `PUI.Web.Shoelace`, `PUI.Web.Fluent`, `PUI.Web.Bootstrap`), all under **src/PUI/Web/** — so the carrier-independent algebra (`PUI`, `Data.Profunctor.*`) stays visibly separate from the web specialization
- **src/PUI/Web/HTML.purs** - the 1-1 HTML vocabulary over the carrier: primitive elements (`text`, `input`, `button`, `div`, etc.); entry `body :: PUI Web {} o -> Effect Unit` (registers the UI component's wiring and feeds nothing — it demands a **closed** app: a pipeline's residual input row is its initial-state obligation, discharged by `with initial`/`mvu seed` down to `{}`, the one self-pointed record, so a forgotten seed is a compile error at the mount point naming the unsupplied fields; the standalone app reads `body $ with initial $ ...`); interaction vocabulary (the collection combinators `foreach`/`edited`/`acted` live at the **PUI level** — see the Acting bullet below): `attrWith :: String -> (i -> String) -> ...` (value-computed attribute — the channel-fed counterpart of static `attr`/`:=`, so a cell's style/coord/colour updates in place through the channel rather than a closure rebuild), `clicked` (click emitter for any element — `button`'s replay-last-value protocol), `provided` (the view-model conditional — the named `Maybe`-valued projection is its argument, content attached and fed on `Just`, detached on `Nothing`; pair with a named `Maybe`-valued projection — `pane # provided currentQuestion` — so the pane consumes the payload, never the whole model, and visibility logic lives in testable business code; detaches, so a pipeline stage, not a gated-merge operand), `clWhen` (value-dependent class — styling, deliberately last-element-only); announcing statics `staticText`, the void `hr` leaf (`{} → {}` chrome) and `staticHTML` (raw-HTML string — internal MDC-chrome use only); the full element-ocular set (`div`/`span`/`table`/`tr`/`td`/`ul`/`li`/`p`/`h1`–`h6`/`img`/`strong`/`em`/`code`/`blockquote`/…, plus the generic `el` for computed/dynamic tags) with attribute/class decorators (`attr`/`:=`, `cl`) — the **SVG** element oculars (`svg`/`circle`/`path`/`text`) live in their own **`src/PUI/Web/SVG.purs`** (`PUI.Web.SVG`), imported qualified (`import PUI.Web.SVG as SVG`) when a UI component needs both the HTML `text` leaf and the SVG `<text>` element; SVG works because `element` is namespace-aware (`PUI.Web.childNS`/`namespaceURI`: `svg` opens the SVG namespace, children inherit, only SVG-namespaced elements use `createElementNS`; HTML stays on `createElement` so MDC component init is unaffected); **structure computed from data at runtime** is `PUI Web` all the way down, no markup DSL. Two regimes: (1) when only *values* change over a **fixed structure** (grids, the SVG canvas), feed the structure as data through the retaining `foreach` and compute each element's content/attributes from its fed value — `text # projected @"value" …` for content, `attrWith` for style/coords, `clicked …` cells emitting via `# toCase @l _.key` to emit identity — so cells are built once and updated in place (no wholesale rebuild, no `data-*`); (2) when an element's *structure* genuinely **varies with the data** (markdown blocks), the closure builders `foreachWith :: (a -> PUI Web {} o) -> PUI Web (Array a) o` (build a whole UI component per element, `el ("h" <> show level)`, wholesale rebuild per feed), `dynamic` (its single-value case; a terminal `displayed (dynamic \m -> each …)` renders a projected structure and passes the model through), and `each xs build` (a fixed closure-known list pinned to `{}`) rebuild per value; each owns its container like `foreach`. `onClickedXY` (pointer-down local/viewBox `{ x, y }`) is the container-level coordinate emitter for canvases; `clicked` (fed value) and `onClickedXY` stay live across feeds — adopt the bare payload with `toCase @l <payloadOf>`. `input`/`textArea` are focus-guarded: model updates never clobber the field being typed in, and the channel stays live. The native elements with a model interface are full **components with canonical rows** (L3): the type-changing `select` (`{ value :: Maybe a } → { value :: a }`, bare `<select>`/`<option>`s, no label of its own), `rangeInput` (`<input type="range">`, the live bounded-quantity slider — `{ value :: { current, min, max, step } }` like the design systems' `sliderLive`), the `progress` display (`<progress>`, `{ value :: Number } → {}`, fraction 0–1) and the `output` status (`<output>`, `[ event :: String ] → {}` — HTML's element for the result of a user action, showing the latest event line in place since plain HTML has nothing self-dismissing); the scalar editors canonicalize in app code via `field @l` (`input "text" # field @"value"`)
- **src/PUI/Web/MDC2.purs** - Material Design 2 components (module `PUI.Web.MDC2`; the full https://m2.material.io catalog as far as MDC Web implements it), a two-sorted vocabulary **structured to mirror `PUI.Web.MDC3`** (same helper shapes — `eventLeaf`/`buttonOf`/`textFieldLeaf` — same definition order, a generic `*Prop` FFI family over the foundation instances instead of one-off wrappers, in-monad `uniqueId`, docs-strict markup; a leaf is the MDC Web catalog's documented markup plus a foundation instance `newComponent material.x."MDCX"` wired through its documented properties/events — text fields write through the foundation's `value` so label float stays foundation-managed, progress displays echo `{}` per feed like `text`). **Components** are UI components with a model interface, each a citizen of exactly one row direction — `×→×` editors (`filledTextField`/`outlinedTextField`/`debouncedTextField` — **canonically labeled** `{ value :: String }` editors, adopted per business field via `# asField @"value" @l` (RecordToRecord: renames `value`→`l`, `dimap`-only; sibling of `field`, which *wraps*; displays adopt via `# forField @"value" @l f` — `lcmap`-only, reads one field through its formatter into the canonical display, `identity` says verbatim, owns no output); likewise `filledTextArea`, `checkbox`, `toggleSwitch`, `slider` (the editor of a **bounded quantity** — the whole business datum `{ current, min, max, step :: Maybe Number }` rides the canonical row, so bounds are model data from the seed, re-scoped at runtime, and the emission is the whole quantity with `current` replaced; emits on **commit** only — one emission per adjustment, so an `updated` fold sees each drag as one transaction; `sliderLive` emits mid-drag for live readouts; same bounded-quantity rows in `PUI.Web.MDC3`/`PUI.Web.Fluent`/`PUI.Web.Bootstrap` sliders and `PUI.Web.Shoelace`'s `rating { current, max }`), `filterChip`, `iconToggle`, and the type-changing `radioButton`, `select`, `segmentedButton` — `{ value :: Maybe a } → { value :: a }`; `tabBar` is the same-type selector with unconditional echo and `MDCTab` activation — the `looped`-ensemble citizen) and the `{ busy } → {}` displays `indeterminateLinearProgress`/`indeterminateCircularProgress` and the determinate `linearProgress` (`{ value :: Number } → {}`, filled fraction 0–1 — the gauge shape `linearProgress # projected @"value" fraction`); the `×→+` events `button` (raised — the high-emphasis default; `outlinedButton`/`textButton` are the other two MD2 emphasis levels)/`fab`/`iconButton`/`menuItem` (canonical `[ clicked :: r ]`, adopted via `# asCase @"clicked" @l` — RecordToVariant, renames the case); the `+→×` statuses `snackbar` (auto-dismissing) and `banner` (own dismiss action) — canonical `[ event :: String ]` in, adopted via `# forCase @"event" @l` (VariantToRecord, renames the incoming case) — no scalar or polymorphic component interfaces (raw scalar leaves are private; `Web` primitives stay scalar for optic positions). `listOf` is the MD2 list as a **dynamic collection component** (`PUI Web a o -> PUI Web (Array a) a`: `foreach`-retained items (reconciled, not rebuilt), MD2 selected styling via the optional `selected` predicate — `listOf {}` selects nothing — each item a click emitter replaying its value). The module header states the **`dimap` round-trip contract** for editors (lossy conversions belong in the model via `settled` after `completed`, not in leaf brackets). **Oculars** (`card { caption }`, `dialog`/`simpleDialog` (modal protocol: **open on feed, close on emission** — feed selectively behind an event case; the content's final stage must emit only on decision, `simpleDialog`'s built-in confirm is a `clicked` pass-through replaying the content's last output), `menu { label }`, `chipSet`, `list`/`listItem`, `dataTable { columns }`/`dataRow`/`dataCell`, `imageList`, `layoutGrid`/`layoutCell`, `topAppBar { title }`, `drawer` (permanent, with a **live nav slot** — nav and content are sibling stages over the same types, both fed, either side's emissions exit), `tooltip { text }`, typography, elevations) are shape-preserving decorators with no model of their own; `divider`/`imageListItem` are announcing statics (`{} → {}` chrome with a face), while `imagePane` is `imageListItem`'s **channel-fed** sibling (`{ src, label } → {}`, `attrWith`-driven), so a gallery is the retaining `foreach` over its pictures rather than a wholesale rebuild. Internally the live leaf is `field @l`-lifted (preferred over raw `focusProperty` for annotation-free inference; runtime-exactness is enforced by the merge gates' `MergeableRecords` trim) with hand-fused chrome (abstract labels can't flow through the merges' `Nub`), while all-chrome groups have concrete rows and stay literal `RecordToRecord.do` merges of announcing chrome (`staticText`/`staticHTML`/`pempty` at `{} → {}`); code order = DOM order
- **src/PUI/Web/MDC3.purs** - Material Design 3 components (https://m3.material.io), the MD3 sibling of `PUI.Web.MDC2` built on Google's official `@material/web` custom elements (`<md-filled-button>`, `<md-checkbox>`, ... — src/PUI/Web/MDC3.js imports the component modules, which registers the tags and adopts the `.md-typescale-*` stylesheet, so a leaf is `element "md-..."` plus property/event wiring: no foundation classes, no hand-fused ripple/label chrome). Same two-sorted vocabulary, same citizenship, and — where the concept survived into MD3 — the same names and signatures, so a demo switches design systems by switching the import; MD3 renames arrive as the honest catalog does: typography is the MD3 typescale (`displayLarge` ... `labelSmall`), `button` (filled) has four sibling emphasis levels (`elevatedButton`/`tonalButton`/`outlinedButton`/`textButton`), elevations are `elevation1/3/5`, and `banner` is gone (MD3 dropped it). M3 catalog entries `@material/web` doesn't implement (segmented button, snackbar, card, top app bar, drawer, data table, image list, tooltip) are hand-rolled as minimal chrome over the `--md-sys-*` tokens, each injecting its stylesheet once via the FFI `ensureStyle`; pages need only the Roboto + Material Symbols Outlined fonts (component styles ship in the bundle — no CSS `<link>` for the design system). Editors follow the leaf-echo protocols of `PUI.Web.MDC2` (`text`-like display echo on the progress displays, focus-guarded text fields, busy-guarded `select`/`tabBar`)
- **src/PUI/Web/Shoelace.purs**, **src/PUI/Web/Fluent.purs**, **src/PUI/Web/Bootstrap.purs** - the non-Material design-system vocabularies, proving bambik a design-system **umbrella**: same two-sorted structure, same citizenship, same leaf-echo protocols (focus-guarded text fields, per-feed display echo, `Just`-only echo on type-changing selectors), and — where the concept exists in both catalogs — the same names and signatures as the MDC modules, so a demo switches design systems by switching the import; each catalog's honest exclusives appear under their own names. `PUI.Web.Shoelace` (module `PUI.Web.Shoelace`; https://shoelace.style, continued as Web Awesome) is the MDC3 recipe verbatim — `@shoelace-style/shoelace` custom elements (`<sl-input>`, `<sl-rating>`, ...) registered by the FFI import, Lit-based so no bind-deferral needed; its exclusive is the star `rating` editor, and the catalog rounds out with `sliderLive` (`<sl-range>`, the live bounded-quantity slider) and the `progressBar` display (`<sl-progress-bar>`); page links the Shoelace light theme CSS from the CDN, icons load from the CDN base path set in the FFI. `PUI.Web.Fluent` (https://fluent2.microsoft.design) wraps Microsoft's `@fluentui/web-components` v3 (`<fluent-button>`, `<fluent-dropdown>`, ...; design tokens set globally from `webLightTheme` at load, so pages need no CSS link); labels associate via `<fluent-field>` wrappers; exclusives: read-only `ratingDisplay`, `messageBar` status; **caveat**: FAST binds a beat after DOM insertion and replays pre-bind property writes at bind (where setters touching bind-time internals break), and its internal update queue is rAF-driven, starving in frameless headless sessions — so the dropdown/radio-group leaves defer writes on a **timer** poll (`whenBoundDo` in src/PUI/Web/Fluent.js) and finish the two starvable registrations (dropdown `insertControl`, group radio collection) themselves; the dropdown's options must be wrapped in a `<fluent-listbox>` (v3's markup contract). `PUI.Web.Bootstrap` (https://getbootstrap.com) is the **CSS-only** member: native elements dressed in documented classes (`form-control`, `form-select`, `form-range`, `form-check form-switch`, `btn btn-primary`, `progress`/`progress-bar`, `toast`, `card`, `list-group`, `badge`), no component JS at all — the page links the Bootstrap 5 stylesheet from the CDN (no npm dep), and the only FFI is the toast's `autoDismiss` timer (what Bootstrap's own JS plugin would do). Shoelace/Fluent keep MDC's `*Prop` FFI family; typography oculars exist only where the catalog has them (Fluent's `title3`/`body1`/`caption1` over `<fluent-text>`; Shoelace and Bootstrap style plain HTML, so the `PUI.Web.HTML` element oculars are the typography)
- **canonical labels are supplied at the call site** - a component is a citizen of one direction with a **canonical row** (L3): `×→×` editors/displays speak `{ value :: _ }`, `×→+` emitters emit `[ clicked :: _ ]`, `+→×` statuses consume `[ event :: _ ]`. That is a vocabulary convention, not a row-profunctor fact, so the nine canonical-row adopters (`projected`/`required`/`forField`/`forProperty`/`asField`, `asCase`/`toCases`, `forCase`/`forCases`) are **label-indexed** in `Data.Profunctor.Row.*` — the canonical label is their first type argument `@c`, and no module hard-codes a literal. Application code names it: `filledTextField # asField @"value" @"name"`, `text # forField @"value" @"points" show`, `button # toCases @"clicked" register`, `snackbar # forCase @"event" @"booked" bookedLine`, `text # projected @"value" f`, `select … # required @"value"`. There is no pinning wrapper anywhere — the convention is written where it is used
- **extras/** - the layer the shape modules stand on, extracted so `Data.Profunctor.Row.*` holds only what mentions a row, laid out **exactly as the ecosystem lays out its own**, and kept outside `src/` under two source roots because claiming an ecosystem module name is a claim about what the module *is*. Everything here is **unreached by the library, the demos and the tests** (L14 would otherwise prune it) but stays in the build glob so it cannot rot; all of it but `Cont` is also non-row and non-carrier, mentioning neither `PUI`, a row nor a carrier.
  - **extras/profunctors/Data/Profunctor/{Resolving,Coresolving,Retaining,Coretaining}.purs** — one *class* per module beside `Data.Profunctor.Strong`/`.Costrong`, so the coined strengths and their co-strengths are four separate files, stated positionally with `Tuple`/`Either`, no row in sight. Pure **complements of the ecosystem's own**: liftable into `purescript-profunctor` unchanged.
  - **extras/profunctors/Data/Profunctor/Cont.purs** — the root's one **carrier**, and so its one member that is *not* liftable: the CPS profunctor `Cont r a b = (b -> r) -> (a -> r)`, the repo's only *pure* carrier of the row algebra — a timeless model where the merge gate is continuation nesting rather than a pair of `Ref`s. Its header inventories, **exhaustively** over every profunctor subclass in the repo and the ecosystem, which classes it validly inhabits (`Strong`, `Choice`, `Category`, `Wander`, `Acting`, `Cochoice`, `RecordToRecord`, `VariantToVariant`, `Monoid r => RecordToVariant`, plus the degenerate `Resolving`/`Coretaining`) and which it provably cannot (`Costrong`, `Coresolving`, `Retaining`, `VariantToRecord`, `Seeding`, `Closed`), each with its reason — so an absent instance is a stated impossibility, never an unwritten one, and those impossibilities are exactly why the trace forms take seeds and `looped` is a primitive. It builds (it was parked until 2026-08-11, which is what let the inventory drift), so a move in the `Data.Profunctor.*` layout that invalidates it is a compile error rather than silent rot.
  - **extras/optics/Data/Lens/{Colens,Coprism,Shutter,Coshutter,Reel,Coreel}.purs** — one *optic* per module beside `Data.Lens.Lens`/`.Prism`, each carrying its type, its collapsed constructor and its `*E` existential encoding at arbitrary `s t a b`. `Colens`/`Coprism` are the optics of the *ecosystem* classes `Costrong`/`Cochoice`, which `profunctor-lenses` never built, so they too are liftable as they stand; `Shutter`/`Coshutter`/`Reel`/`Coreel` are coined class and optic alike, so each would travel with its class. Plus **extras/optics/Data/Lens/Prism/Existential.purs** (`prismE`, the existential constructor of the ecosystem's own `Prism`, which `Data.Lens.Prism` does not export — it *extends* that family rather than shadowing it, which is why it is not named `Data.Lens.Prism`): the purest complement in the tree, since both the optic and its `Choice` are already the ecosystem's.
  - **extras/row-profunctors/Data/Profunctor/{Row,Row/*,Acting,Seeding}.purs** — the **row-profunctor algebra itself**, and a different claim from the two roots above: not anyone's complement but bambik's own invention, yet still carrier-agnostic (the algebra of merging labelled rows; `PUI` is one carrier that satisfies it, `(->)` another for the value-level laws). With this root out of `src/`, **`src/` holds exactly the carrier and its vocabularies** — `PUI`, `PUI.Trace`, `PUI.Web` and `PUI.Web.*` — so the split reads: `src/` is the UI library, `extras/` is the algebra it stands on.
  - All three roots are covered by the single glob `extras/**/*.purs` in spago.dhall's `sources` beside `src/**/*.purs`, and watched by scripts/dev.mjs. **Downstream caveat**: spago globs a git dependency as `.spago/<pkg>/<ver>/src/**/*.purs` — hardcoded, ignoring the package's own `sources` (the same reason the bootstrap must spell out the dependency list) — so modules outside `src/` are invisible to a consuming app. A tag carrying this layout therefore needs the app's own `sources` to add `.spago/bambik/<tag>/extras/**/*.purs`, which is why bootstrap.md's spago.dhall step carries that second glob; one glob covers all three roots, and it is tag-pinned, so it moves with `bambik.version`. The row layer's combinators are these optics at row granularity (`feedback` a `Colens`, `iterate` a `Coprism`, `folding` a `Coshutter`, `unfolding` a `Coreel`, `subResolving`/`subRetaining` a `Shutter`/`Reel`)
- **extras/row-profunctors/Data/Profunctor/** - `Seeding` (**pointedness as carrier structure**: `class Category p <= Seeding p` with `seeded :: a -> p a a`, the pointed wire — identity plus one registration emission of the seed; the primitive behind `with`/`announce` and the trace forms' seed arguments; deliberately no `(->)` instance — a timeless carrier has no registration moment) + the `Row/` layer; everything else was dissolved or deleted
- **extras/row-profunctors/Data/Profunctor/Row/** - Row profunctors over `Record`/`Variant`: four direction modules, each carrying its **direction class** — the binary merge plus its nullary unit `pempty`, the genuine per-carrier primitives — with qualified-do sugar (`bind`/`discard`). Everything kept is demo-reachable; laws are stated in the module headers. Type variables follow the photographic schema: focus `f`, background `b`, shot `s` (`Cons l f b s`), reality `r`.
  - **`RecordToRecord.purs`** (×→×) — merge `recordToRecord` (`SharedRecordInputs` + `OwnedRecordOutputs`; gated on `PUI`); over ecosystem `Strong`: `subStrong` (sub-record focus, background carried), `focusProperty` (the type-changing field lens); over bare `Profunctor`: `atProperty` (`atField`'s open-row sibling — feed a structural UI component the bare field `l` of a wider row, exactly as `forProperty` is `forField`'s open-row sibling), `toField` (introduce a **bare** output as field `l` — `rmap`-only, the transpose of `toCase`, and the output half `field` fuses: `field @l = atField @l <<< toField @l identity`; the output-side canonical rename is `toField @l _.value`, as `asCase` is `toCase` at the canonical eliminator), `field` (`focusProperty`'s closed-singleton form — the merge-operand/nesting shape, `dimap`-only, annotation-free under the merges; runtime-exactness is enforced by the merges themselves — their `MergeableRecords` evidence trims every operand emission to its declared output row before the gates' left-biased `Record.union`, so lens-rebuilt emissions carrying stale sibling fields can't shadow fresh values); `required @"value"` (marks a type-changing selector as always-selected — dissolves the `dimap (\v -> { value: Just v }) _.value` bracket: `select config options # required @"value" # asField @"value" @l`); `forProperty` (read field `l` of a **wider** row into the canonical `{ value }` display — `projected @"value"`'s label-indexed one-field form and `forField`'s open-row sibling, the display-side `focusProperty`: for context-pinned rows — collection items, pane payloads — `text # forProperty @"value" @"label"`; `forField`'s closed singleton remains the merge-operand form); `tapped` (a `Strong`-derived display tap: shows the value flowing through a pipeline stage and passes it on — the display's echo triggers the forwarding; honest over displays only; **record-typed and subsuming**, so `text # projected @"value" readout # tapped` works with `readout` closed at just the fields it formats); `completed` (complete a UI component's output to its full input row over `Strong` — unproduced fields carried from the retained input, `FieldNames`-trimmed union, absorbs the input widening; kills `field @l identity` echo wires); over ecosystem `Costrong`: the row form `feedback` of the **`Colens`** optic (`Data.Lens.Colens`) (the ×-trace at row granularity — a state sub-record loops from output to input; coerce-split like `subStrong`, so pipeline stages only; takes the traced chain's **initial state** `{ | iw }` as its first argument — the chain is an entity, fed its t=0 value at registration via a composed `seeded` wire, so the loop is primed before any input and never starves).
  - **`VariantToVariant.purs`** (+→+) — merge `variantToVariant` (`OwnedVariantInputs` — one handler per case — + `SharedVariantOutputs`); over ecosystem `Choice`: `focusCase` (the value-level case prism, via `prismE`), `subChoice` (**sub-variant focus**, `subStrong`'s transpose completing the wrap family's `+→+` corner: the wrapped profunctor handles the focus cases, background cases pass untouched — cashbox's money events detour through confirmation dialogs while its audit event flows straight to the fold) and `splitVariant` (the dispatch helper `VariantToRecord.subRetaining`, `iterate` and `subChoice` share); over bare `Profunctor`: `atCase` (adopt a bare-input UI component as the owner of input case `l` — the closed-singleton unwrap at `+`, `atField`'s exact transpose); over ecosystem `Cochoice`: the row form `iterate` of the **`Coprism`** optic (the `+`-diagonal trace at row granularity — `again` cases loop back into the input, `done` cases exit; retry/wizard flows).
  - **`RecordToVariant.purs`** (×→+) — strength `Resolving`/`resolve :: p a b -> p (Tuple a c) (Either b c)` (a loop step: `Left` = Done, `Right` = Loop; `PUI`-only instances — the branch is derived **from time**: emissions loop while the UI component is still moving, the last resolves at quiescence, so `coresolve (resolve g) = debounced g`; window parameterized via `resolveFor`) and its co-strength `Coresolving`/`coresolve` (ties the loop: a terminating fold) with its row form `folding @w` (case `w` continues the fold silently, `done` cases exit — the accumulating-wizard shape; takes the fold state's **initial value** `{ | fb }` as its first argument, emitted once as case `w` at registration via a composed `seeded` wire, so the fold never starves); merge `recordToVariant` (ungated broadcast); over `Resolving`: `subResolving`, `focusProperty`, `backgroundProperty`, and over `Coresolving` the row form `folding` of the **`Coshutter`** optic; plus `recordToCase` (introduce an output case; plain `Profunctor`, the unit-pinned merge by law), `toCase` (`recordToCase` freed of the record-input constraint at the closed singleton row — introduce a **bare** output as case `l`, the output-side dual of `atCase` and the general sibling of `asCase`; dissolves the `rmap`-style payload lambda at collection sites: `listOf {} entries item # toCase @"picked" _.key`), `toCases @"clicked"` (adopt the canonical click case into the **variant of business outcomes** `f` computes — `button # toCases @"clicked" register` emits `register`'s cases directly; row-typed on the outcome side, since a non-variant result is out of shape at `×→+`; the output dual of `VariantToRecord`'s `forCases @"event"`).
  - **`VariantToRecord.purs`** (+→×) — strength `Retaining`/`retain :: p a b -> p (Either a c) (Tuple b c)` (a Mealy/coroutine step; `PUI`-only instances — a stateless function can't retain state) and its co-strength `Coretaining`/`coretain` (ties the state channel: a productive unfold/generator) with its row form `unfolding @w` (value fields pass, state fields resume as case `w`; takes the unfold state's **initial value** `{ | fb }` as its first argument, fed once as case `w` at registration, so a gated `retain` inside the chain is primed before the first fresh input); merge `variantToRecord` (gated like ×→×, retaining the other side's last contribution); `forCases @"event"` (adopt a status for a **whole classified variant** — one copy classifier renders every case into the canonical `event` payload, so a single status instance serves mutually exclusive outcomes: `snackbar # forCases @"event" (match { booked: …, rejected: … })`; `toCases @"clicked"`' input dual and `forCase @"event" @l`'s plural); over `Retaining`: `subRetaining`, `focusCase`, `backgroundCase`, `reduceCase` (Mealy reducer), and over `Coretaining` the row form `unfolding` of the **`Coreel`** optic.
  - the collection lives one level up, in **`extras/row-profunctors/Data/Profunctor/Acting.purs`** (module `Data.Profunctor.Acting` — beside `Row/`, not under it: rows are the finitary μ-free fragment of the container grammar, `Array = μx. 1 + a×x` is one `μ` later). `class Profunctor p <= Acting p where actedBy :: Ord k => (a -> k) -> p a b -> p (Array a) (Array b)` (the minimal carrier primitive) with the vocabulary form `acted @l :: Strong p => Cons l k ra a => Cons l k rb b => Lacks l rb => Ord k => p {|a} {|rb} -> p (Array {|a}) (Array {|b})` (like `edited`, the element's output row **excludes the key** — re-attached from the input row via the Strong state channel, so identity is unforgeable) — the **container action** (Tambara for `Array`, generated by `Strong`+`Choice`+μ), keyed by the row's **materialized identity field** (the element is a data-model row; rows carry their identity; `Ord k` is the reconciler's Map-indexing requirement — identity semantics remain equality; keys must be unique, never rendered). Laws in the header: **empty** (fed `[]` emits `[]` — announcing product-output unit; nothing at registration), **singleton retraction** (yanking at the container), **gather gate** (`Array b` withheld until every element spoke, retain-last thereafter), **identity-follows-key** (stateful carriers). The module holds only the **pure algebra** — the class, `instance Acting (->)` (`actedBy _ = map` — laws value-testable), and `optioned`; the carrier machinery lives with the carriers, exactly like the merge instances: **`PUI.purs`** carries `class Hosting m node | m -> node` (what a stateful carrier contributes — instantiate one element UI component at runtime + placement: detach a leaver, restack survivors), the placement-free `Hosting Effect` instance (the probe carrier the `spago test` laws run on), the shared keyed reconciler, the one **generic** `instance Hosting m node => Acting (PUI m)`, and the two other collection combinators, all `Hosting`-generic: `foreach @l :: Cons l k r a => Ord k => PUI m {|a} o -> PUI m (Array {|a}) o` (the collapsed/sum-flavored form — **keyed and retaining** reconciliation, matched elements re-fed in place, nodes moved with their keys; forwards each element emission onto the shared channel, ungated, silent on empty; written trailing in a container ocular, `ul $ item # foreach @"id"`) and `edited @l :: Cons l k r a => Lacks l r => Ord k => PUI m {|a} {|r} -> PUI m (Array {|a}) (Array {|a})` (the **collection editor** — element emissions folded back in by key, whole array emitted **immediately**, input-primed: the retained fed array supplies unedited slots; the key is a **label**, not a function, and the element's output row **excludes** it — the carrier re-attaches each emission's key as the edit's return address, so an element structurally cannot change its key, and the old `# completed`-the-id-through convention dissolves — the element widens its input instead, `merge # widenRecordInput # edited @"id"`). The **keyed-input members** complete the direction square — their input is the runtime variant `{ key, value }` (one case at a time, the tag arrives in the input, so no key function; `Ord k` tags): `dispatched :: Ord k => PUI m a b -> PUI m { key :: k, value :: a } { key :: k, value :: b }` (+→+ — an unknown key instantiates a new case, a known key re-feeds exactly its instance, emissions leave tagged; the targeted-update/stream direction, no whole-array re-feed) and `accumulated :: Ord k => PUI m a a -> PUI m { key :: k, value :: a } (Array a)` (+→× — the keyed Mealy: grows per new key in first-appearance order, updates known slots, emits the whole array immediately, input-primed; the board/ledger shape); the +-members never detach/restack (absence of a key is no signal — removal/ordering stay array-level concerns upstream). Key forms encode the key's ontology: a **label `@l`** on the ×-members (identity = a materialized model field; in `edited` the output row excludes it and the carrier re-attaches it), the **`{ key, value }` envelope** on the +-members (identity = the structural tag, arriving in the input). The five divide the ground by input (everything at once → ×; one entity at a time → +) and output — individual event → `foreach`/`dispatched`; aggregate as joint decision (emission-primed gather) → `acted`; aggregate as running state (input-primed, immediate) → `edited`/`accumulated`; comparison table in `PUI.purs`'s container-action section. **`PUI.Web`** carries `instance Hosting Web Node` (DOM placement — `appendChild` moves nodes, so identity follows the key). `optioned` derives the `Maybe = 1 + a` action via the Array embedding. Design note: doc/collections-profunctor-algebra.md.
  - **`Row.purs`** (module `Data.Profunctor.Row`) — the shared floor: the row-constraint vocabulary (`InclusiveRows`/`ExclusiveRows` + the runtime-evidence duals `DispatchableVariants`/`MergeableRecords` with `exactRow`, bundled into the per-side classes `SharedRecordInputs`/`SharedVariantOutputs`/`OwnedVariantInputs`/`OwnedRecordOutputs` — sharing is inclusive, responsibility is exclusive, evidence only on owned sides — so each merge signature is two words, one per side; the owned sides also carry the `DisjointLabels` detector, which turns a duplicated label into a custom compile error naming the label) + the two `dimap`-only widening reshapings (`widenRecordInput`/`widenVariantOutput`) the `PUI` merge instances build on — `widenRecordInput` is **library plumbing, not vocabulary**: it is deliberately not re-exported from `PUI`, because record **subsumption is baked into the stages that read a row** (`tapped`/`displayed`/`updated`/`every`/`edited`/`acted`, plus `completed`), so a demo states each business function's exact footprint as a closed row and never coerces at the call site. It is *not* baked into `foreach` (the element row is inferred from the array), `provided` (the pane is fed the payload) or `atCase` (a case payload is pinned by its consumer as often as by its emitter) — **rows are read narrow, payloads are exact**. The no-nominal-types-in-UI rule (every view-model type anonymous and structural) is stated for application code in `.claude/skills/developing-bambik-apps/writing.md`.
  - **`test/Main.purs`** — value-level `(->)` tests (`subStrong`/`focusProperty`/`recordToCase`) plus merge unit laws, gating, and the trace quartet and its row forms (`unfirst`/`unleft`/`coresolve`/`coretain`/`looped`/`iterate`/`feedback`/`folding`/`unfolding`) on the `PUI` carrier via a probe harness; **`test/HelloShutterReel.purs`**/**`BusinessOptics.purs`**/**`RestaurantReel.purs`**/**`EntityEventExample.purs`** — `Shutter`/`Reel` as business optics.

### Composition Patterns

- `Semigroupoid.do` (qualified-do) - Data flow pipelines
- `RecordToRecord.do` / `RecordToVariant.do` / `VariantToVariant.do` / `VariantToRecord.do` (qualified-do) - the four row merges
- label-indexed components - every MDC component is a citizen of one direction (`filledTextField @l` ×→×, `button @l` ×→+, `snackbar @l` +→×), so pipeline stages are written directly from components; `field`/`subStrong` nest sub-composites into larger aggregates
- variant editing - **record-shaped editor state**: the model keeps the variant, the editor keeps every payload; `bracketed <stateOf> <caseOf>` wraps `Semigroupoid.do { selection component # completed; payload panes }` (the variant in via a state function seeding absent payloads, out via a projection on the selection, self-traced in between) — each pane a payload-typed editor stage `# provided <paneOf> # updated <setPane>`, shown by the presence of its `Maybe` payload; consistency via the self-trace re-broadcast; unit-payload variants need only the bracket around one selection component
- conditional visibility - `provided <maybeOf>` with a named `Maybe`-valued projection (never an in-UI predicate), or `providedCase @l <variantOf>` for case-gated existence — a stored variant field (`# providedCase @"serving" identity # atField @"display"`) or, for **mutually exclusive derived states**, one variant-returning classifier per rule family (`# providedCase @"taken" usernameStatus`, signup-form: two classifiers replaced five `Maybe` projections, exclusivity by construction): conditional *data* (a pane whose content only exists sometimes) → `pane # provided <maybeOf>`; conditional *mode of a live editor* inside a `looped` ensemble → a payload pane as above. `clWhen` stays predicate-driven — it toggles a class (styling), not visibility
- `tapped` - live views as pipeline stages (slider readouts, summary lines, data tables): display every emission flowing through, pass it on; the read function is closed at its own footprint (`tapped` subsumes)

### Separation of Concerns

- **Business Logic** - Row-shaped models, structural throughout, per the application code-style contract in **`.claude/skills/developing-bambik-apps/writing.md`** (see the note at the top of this file — it is the single normative statement, and demos are its executable form): no nominal types in UI, exact single-record footprints dispatched with `informed`, no business literals in UI code, no pass-through fields, emissions carrying bare data. Below the UI sit plain functions and Aff actions (demo/nguis/order-form-mdc2/OrderFormMDC2.purs), nominal types only where recursion (cells' `Expr` AST — rows can't express μ) or an ecosystem API (`Aff`, `Either`, `Milliseconds`) demands them, and business optics (Shutter/Reel) where state/loop semantics are needed (test/BusinessOptics.purs)
- **Design System** - Oculars in the vocabulary modules (`PUI.Web.HTML`/`SVG`, MDC2, MDC3, Shoelace, Fluent, Bootstrap); the type `Ocular p = forall a b. Optic p a b a b` is declared in **src/PUI.purs** beside its sibling `Action` — the optic transpose (fix the carrier, quantify the data), with its admission law in the header
- **Composition** - UI elements compose orthogonally to the row combinators

## Demo Structure

- **demo/7guis/** - The seven [7GUIs](https://eugenkiss.github.io/7guis/) benchmark tasks, one page each (counter, temperature-converter, flight-booker, timer, crud, circle-drawer, cells), all on the named MVU shape `pipeline # mvu seed`; circle-drawer's SVG canvas and cells' 31×27 spreadsheet grid are **channel-fed structure-from-data** in `PUI Web` (a fixed canvas/grid fed as data through the retaining `foreach` — cells/circles built once and updated in place via `attrWith` + `text`, so an edit/selection/resize never rebuilds the ~800 cells or redraws the canvas; circle-drawer uses container-level `onClickedXY`, cells emits its clicked-cell key via `clicked …` cells emitting via `# toCase @l _.key` — no `data-*`, no markup DSL), `MDC2.listOf` for crud's list, an anonymous `[ oneWay :: {}, return :: {} ]` variant row through the type-changing `select` (`# required @"value" # asField`) in flight-booker — whose booked/rejected outcomes carry bare payloads (the itinerary, the problem) into **one** snackbar, `snackbar # forCases @"event" bookingLine` inline at the pipeline tail, the `forCases @"event"` showcase (smoke: scripts/smoke/tests/flight-booker.mjs) —, circle-drawer's live-preview resize via `sliderLive` with an `adjusting` flag coalescing each drag session into one undo transaction, a both-fields model in temperature-converter (non-numeric input leaves the other field untouched) and a hand-rolled formula evaluator in cells; each page shows its own source beside the running demo. Verify with `npm run smoke` (the committed scripts/smoke/ harness). The 7GUIs suite exists in **all six vocabularies** — beside each `-mdc2`/`-mdc3` pair sit `-shoelace`, `-fluent`, `-bootstrap` and `-html` siblings over the same logic modules (`counter-shoelace`/`CounterShoelace`, …, `counter-html`/`CounterHTML`), the per-variant diff being the honest catalog mapping: vocabularies without `listOf` build crud's selectable list as a keyed `foreach` of `clicked` rows (Bootstrap styles selection with the documented `active` class via `clWhen`, the rest with an `attrWith` style), vocabularies without an indeterminate progress run the Aff stages as `silence # action …`, and the `-html` variants are the **plain-HTML floor**: each mounts one container `div` (so `provided` panes re-attach inside the demo's own DOM), scalar leaves are canonicalized in place (`input "text" # field @"value"`, `# field @"formula"`), captions ride `label`+`staticText` merges, and flight-booker runs the native `select` and lands both outcomes in the native `output` status (smokes: counter-{shoelace,fluent,bootstrap,html}, timer-{shoelace,html}, flight-booker-html)
- **demo/nguis/** - Companion set of popular showcase apps, same conventions as 7GUIs (named modules entered at their own function, per-page source panels): order-form is the **four-direction showcase** and the suite's flagship (the former standalone demo/1) — full MDC order form as one pipeline through every row direction: load action → `×→×` form (nested record merges; variant editors as `bracketed` pipelines of `tabBar`/`segmentedButton` + `provided` payload panes) → `tapped` live summary → `×→+` event buttons → `+→+` backend dispatch → `+→×` status snackbars; smoke-tested in scripts/smoke/tests/order-form.mjs. Then todomvc (`listOf` click-to-toggle, `clWhen` done-styling, `segmentedButton` filter), tip-calculator (all-`×→×` form, sliders, `tapped` money readouts), quiz (`provided` panes over multi-stage pipelines keyed on `Maybe`-projected question/outcome, `linearProgress`, `listOf` choices), tic-tac-toe and calculator (**channel-fed `foreach` grids** — cells built once, content via `text`, style via `attrWith`, key emitted via `clicked` + `toCase @l _.key`, folded by `updated`; no `dynamic` rebuild, no `data-*`; calculator's keypad is static keys fed once through the retaining `foreach`, readout channel-fed), markdown-previewer (`filledTextArea` + injection-proof preview as recursive `PUI Web` — `displayed (dynamic \doc -> each …)` over element oculars with `el ("h" <> show level)`, structure genuinely varies per block so it stays builder-built — over a hand-rolled parser), helloworld (`body $ staticText` — the 5 kB bundle floor), stopwatch (`every` tick pausing via `Nothing` over a stored phase variant `[ halted :: {}, timing :: {} ]` — a Boolean nobody edits as a Boolean is a phase — with button panes `# providedCase @l stopwatchPhase` (the trivial read's closed signature stating the row), laps a keyed `foreach @"line" lapLines … # displayed` (lap lines materialized as `{ line }` rows)), reorder (the keyed-reconciliation showcase and the `edited` collection-editor demo: a playlist keyed by track id, each row a bare DOM-local checkbox plus an in-row rename field — `(RecordToRecord.do …) # widenRecordInput) # edited @"id") # field @"order"`, the element's output row excluding the id (the carrier re-attaches it); Rotate and the effectful Shuffle — `button # asCase` → `silence # action … # atCase`, `Effect.Random` — both move each row's DOM node with its track so the tick, the renamed title, and focus all follow it), shopping-cart (`dataTable`/`dataRow`/`dataCell` over `foreach`, a `listOf` catalogue fed by its `productCatalogue` projection argument), password-generator (effectful stage: `button # asCase` → `action`/`atCase` with `Effect.Random` → `updated`), color-mixer (`sliderLive` channels driving an `attrWith` swatch + static `foreach` preset chips). The **trace-quartet row forms** each get a focused single-combinator demo here: auction (`feedback`, `Costrong`: a `top`-bid field loops output→input, the chain's initial state passed as `feedback`'s seed argument — the running high-water-mark, invisible in the stage's outer type), checkout (`folding @"next"`, `Coresolving`: a 3-step wizard whose step state loops silently as the `next` case, seeded with the first step as `folding`'s argument, exiting on the `placed` done-case), payment (`iterate`, `Cochoice`: a flaky card charge whose failed attempt re-emits `charge` with attempt+1 until it succeeds — a control loop, so no seed: events occur, they don't pre-exist; also the **`observed` showcase**: `snackbar # forCase @"event" @"charge" retryLine # observed` sits inline in the `iterate` chain and narrates each declined attempt while the event passes on into the loop — smoke: scripts/smoke/tests/payment.mjs), ticket-dispenser (`unfolding @"resume"` + the `Reel` optic, `Coretaining`: "take a number" — each press emits the next ticket, the counter seeded as `unfolding`'s argument and resumed as the `resume` case; also the **`providedCase` showcase**: the counter's state is a payload-carrying variant field `display :: [ waiting :: {}, serving :: { number :: Int } ]`, so the panes are pure case adoption — `pane # providedCase @"serving" identity # atField @"display"` — no `Maybe` projections); the three seeded forms are smoke-tested on live pages in scripts/smoke/tests/{auction,checkout,ticket-dispenser}.mjs (seeded render at t=0, the full wizard walk, two ticket draws). The **sub-structure focus pair** gets the same treatment: parcel (`subStrong` — a flat `{ recipient, street, city }` model, the reusable address sub-form a citizen over its own closed `{ street, city }` row lifted `# subStrong` as a pipeline stage, the background field threaded; smoke: scripts/smoke/tests/parcel.mjs) and cashbox (`subChoice` — selective interception as UX: the outgoing money (refund, payout) detours through confirmation dialogs, each `simpleDialog … # tapped) # atCase @l # toCase @l' identity` — the `+→+` closed-singleton wrap in merge position — while the incoming deposit posts straight to the fold, so every branch is exactly `informed`-dispatched `{ amount, balance } -> { balance }` (A12/A13); buttons carry their payloads via `button { … } # with patch # asCase @"clicked" @l`; smoke: scripts/smoke/tests/cashbox.mjs, which polls the MDC dialog's rAF-driven open since the harness starves rAF for seconds). potluck is the **container-action demo** (`acted` from `Data.Profunctor.Acting`: a fixed guest list under a `{ guests :: Array … }` model lifted with `# field @"guests"`, each row a `RecordToRecord.do` merge of the name display and a per-guest `segmentedButton` dish editor, lifted with `# acted @"name"`; the menu summary is **withheld by the gather gate until every guest has chosen**, re-emits whole on any re-choice, and row DOM nodes survive every re-feed — the knowledge gate as UX; it also shows `foreach` beside `acted`: the menu itself is a keyed `foreach` of per-guest fragments, each a `RecordToRecord.do` merge of `text`/`staticText` pieces, so a re-choice updates exactly one text node; smoke-tested in scripts/smoke/tests/potluck.mjs). departures and scoreboard are the **keyed-input demos** (a deterministic `every` ticker synthesizes one `{ key, value }` case per second; departures: `dispatched` — flight rows appear on first mention and re-feed in place, the tagged output drives a last-update line; scoreboard: `accumulated` — the board grows to its key set, points update in place, the emitted whole array drives the standings line; smokes assert case-instantiation, in-place re-feeds via node identity, and the aggregate). stopwatch's `laps # foreach @l lapLines … # displayed` tail reads through the duoidal lens: `displayed` is the comonoid (comultiplication) a `⊳`-pipeline tail requires, not a workaround. restaurant-menu is the **plain-HTML showcase** (no MDC at all — the successor to the retired demo/2): a fancy static fine-dining menu built purely from element oculars (`el`/`h1`–`h3`/`p`/`span`/`ul`/`li`/`blockquote`/`svg`/`circle`/`a`/`hr`) with `cl`/`:=` decorators and `staticText` leaves, siblings merged with `RecordToRecord.do` of `{} → {}` announcing chrome, courses/dishes/tags from data via `each` (each `each` owns its own container — the top one is wrapped in its own `div` so it doesn't `removeAllChildren` its static siblings), seeded once with `with {}`; the fine-dining look is ordinary CSS on the page, structure is bambik. espresso-bar is the **`PUI.Web.MDC3` (Material Design 3) showcase**: an espresso-bar order form on the signup-form shape — a `mvu`-looped `RecordToRecord.do` of `tabBar`/`filledTextField`/`segmentedButton`/`select`/`radioButton`/`sliderLive`/`chipSet`+`filterChip`/`toggleSwitch`/`iconToggle`/`tooltip`+`checkbox`, a presets `menu` of `menuItem`s folded via `updated`, then `tapped` summary + `linearProgress` gauge stages, `button # asCase @"clicked" @"brewed"` → an inline toast stage (`snackbar # forCase @"event" @"brewed" brewedLine` — the copy is a named business line from the logic module, the business event carries the bare order); smoke-tested in scripts/smoke/tests/espresso-bar.mjs (host-level events on the custom elements); its MDC2 twin is espresso-bar-mdc2. order-dashboard is the **custom-components showcase** (MDC3-only, no twin): the demo directory ships its own packaged components module beside the app (demo sources are spago-globbed, so a demo can carry any number of modules) — `DashboardControlsMDC3` defines five dashboard controls over `PUI.Web.MDC3` + HTML/SVG plus a `board` layout ocular, each control a citizen of one direction with MDC-canonical rows: the displays `statTile { label, unit }`/`gauge { label }` (`{ value } → {}`, chrome merged with `text`/`linearProgress`), `trendChart` (channel-fed SVG sparkline — `attrWith "d"` over a `pempty # constantly {}` leaf, so it announces at registration), `leaderboard` (keyed `foreach` list lifted `# atField @"value" # displayed` **inside** its chrome merge — the packaged-collection-display protocol: the control is an unconditional pass-through `{ value :: Array … } → { value :: Array … }` and an empty array neither starves the gate nor trips the watchdog), and the selector `rangePicker` (`{ value :: Maybe a } → { value :: a }`, `segmentedButton` under a label); `OrderDashboardMDC3` composes them into an online order system dashboard driven by a deterministic simulated order stream (`every { ms: 1000.0 }` + pseudo-random arrivals, seeded with a 15-minute history) through `# projected @"value" f # displayed` tile stages behind a window picker `# required @"value" # asField @"value" @"window" # completed`; smoke-tested in scripts/smoke/tests/order-dashboard.mjs (seeded stats, window re-scoping, stream growth by polling). product-review, meeting-booker and loan-calculator are the **design-system-umbrella showcases**, one per non-Material vocabulary (suffixed like the MDC demos: `product-review-shoelace`/`ProductReviewShoelace`, `meeting-booker-fluent`/`MeetingBookerFluent`, `loan-calculator-bootstrap`/`LoanCalculatorBootstrap`; no MDC2|MDC3 switcher — the suffix names the vocabulary, not a twin): product-review (`PUI.Web.Shoelace` — the espresso-bar shape around Shoelace's exclusive star `rating`, `sl-alert` toast on submit; smoke: scripts/smoke/tests/product-review.mjs), meeting-booker (`PUI.Web.Fluent` — `dropdown`/`radioGroup`/`slider`/`toggleSwitch` form with `ratingDisplay` + `progressBar` gauge stages following the room and a `messageBar` booking toast; also the **no-defaults showcase**: nothing is pre-picked — room and duration are `# optional # asField @"value" @l` selectors over `Maybe` model fields seeded `Nothing`, the attendees slider is a `provided` pane whose bounded quantity is *born* with the chosen room's capacity (`seatsFor` attaches bounds in the pane projection — the room/capacity invariant holds by existence, no normalization), the room gauges and the plan/book stage are `provided` panes keyed on `Maybe` projections (`ratedRoom`, `completePlan`), so an incomplete meeting is unbookable by construction — the knowledge gates as UX; its smoke, scripts/smoke/tests/meeting-booker.mjs, deliberately exercises the timer-based bind deferral since the harness is frameless), loan-calculator (`PUI.Web.Bootstrap` — all-`sliderLive` annuity form, `listGroup` readouts with a `badge` payment figure, `progress` interest-share gauge, `toast` on apply; smoke: scripts/smoke/tests/loan-calculator.mjs)

- **MDC2/MDC3 sibling demos** - every design-system demo exists in both vocabularies, in explicitly suffixed sibling directories (`demo/7guis/counter-mdc2/` with module `CounterMDC2`/entry `counterMDC2`, `demo/7guis/counter-mdc3/` with `CounterMDC3`/`counterMDC3`). Every demo follows writing.md's **view/logic module separation**: the demo module is the view, importing a `<Demo>Logic` module of the pure business functions — twins share that logic module verbatim from the **unsuffixed sibling directory** (`demo/7guis/counter/CounterLogic.purs`, compiled via the spago glob, fetched by the pages as `../counter/CounterLogic.purs` in `data-source`), single-variant demos keep it beside the app in the same directory, and helloworld (all view) has none — so the MDC3 twin is the same view module with the import switched to `PUI.Web.MDC3` and only the honest vocabulary mapped, the twin diff being view-only by construction (typography per the Material migration guide — `headline4`→`headlineLarge`, `caption`→`bodySmall`, `overline`→`labelSmall`, …; `elevation10/20`→`elevation3/5`; inbox's `banner`→`snackbar`), page head swapped to the MD3 fonts — the per-demo diff *is* the design-system switch. Single-variant demos carry no suffix (helloworld, restaurant-menu — no MDC at all); espresso-bar, born MDC3, has its twin generated in reverse (espresso-bar-mdc2 — `bodyMedium`→`body2`, `labelMedium`→`caption`, `elevation5`→`elevation20`, Symbols-only glyphs swapped for Material Icons ones). All registered in scripts/demos.mjs, linked from the suite indexes as `Name (MDC2 | MDC3 | …)` (the 7GUIs index lists all six vocabularies), and every suffixed page header carries a **design-system switcher** injected by demo/page.js from the path convention (suffix family mdc2/mdc3/shoelace/fluent/bootstrap/html — every sibling probed with a HEAD request, the switcher listing exactly the ones that exist; unsuffixed pages get none); counter-mdc3 is the representative MDC3 smoke, counter-{shoelace,fluent,bootstrap,html} the representative non-Material ones, the other per-demo smokes walk the -mdc2 pages. **Naming rule**: MDC2/MDC3 (Material Design Components) name the component vocabularies, modules, directories and UI labels; plain MD2/MD3 (Material Design) is reserved for the design-system specs/catalogs (m2/m3.material.io) in prose.

## Key Dependencies

- `profunctor-lenses` - Profunctor-based optics
- `qualified-do` - Syntax sugar for profunctor composition
- `material-components-web` - MDC (Material Design 2) JavaScript library
- `@material/web` - Material Design 3 web components (custom elements, used by `PUI.Web.MDC3`)
- `@shoelace-style/shoelace` - Shoelace/Web Awesome web components (used by `PUI.Web.Shoelace`; pages link its light theme CSS from the matching CDN release)
- `@fluentui/web-components` - Fluent UI v3 web components (used by `PUI.Web.Fluent`; theme tokens ship in the bundle)
- Bootstrap is CSS-only and **not** an npm dependency — `PUI.Web.Bootstrap` is native elements + classes, pages link the Bootstrap 5 stylesheet from the CDN
